import buildSrc.Plugin
import buildSrc.Url
import buildSrc.Path

apply from: Path.baseProject

buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath Plugin.androidGradle
        classpath Plugin.kotlin
    }
}

allprojects {
    repositories {
        jcenter()
        google()
        maven { url Url.mavenLocalInternal }
        maven { url Url.maven }
    }

    disableLintTasks(it)
    ignoreRelease(it)
}

task clean(type: Delete) {
    group 'Cleanup'
    doLast {
        delete rootProject.buildDir
    }
}